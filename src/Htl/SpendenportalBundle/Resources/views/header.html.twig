<!DOCTYPE html>
<html lang="de" data-ng-app="SpendenverbindetApp">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="shortcut icon" type="image/x-icon" href="{{ asset('bundles/htlspendenportal/img/fav_icon.png') }}">

    <title>{% block title_text %} Spendenverbindet {% endblock %}</title>

    {% stylesheets filter="scssphp" output="css/app.css"
        'bundles/htlspendenportal/bower_components/uikit/css/uikit.min.css'
        'bundles/htlspendenportal/bower_components/uikit/css/components/slideshow.css'
        'bundles/htlspendenportal/bower_components/uikit/css/components/progress.css'
        'bundles/htlspendenportal/bower_components/uikit/css/components/slidenav.min.css'
        "bundles/htlspendenportal/sass/styles.scss"
    %}
    <link rel="stylesheet" href="{{ asset_url }}" />
    {% endstylesheets %}

</head>

<body data-ng-controller="spendenverbindetController" >

<!-- === navbar === -->
<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">

            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <a class="brand" href="/">
               <img class="sv-nav-logo" src="{{ asset('bundles/htlspendenportal/img/logo.png') }}" width="200" alt="logo">
            </a>

        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <!-- <ul class="nav navbar-nav navbar-right">  old version -->
            <ul class="nav navbar-nav sv-remove-margin">

                {% if is_granted('ROLE_RECEIVER') == false %}
                    <li><a id="entdecken_id" href="/entdecken">ENTDECKEN</a></li>
                    <li><a href="#">BESTENLISTE</a></li>
                {% endif %}
                {% if is_granted('ROLE_DONATOR') %}
                    <li><a id="abonniert_id" href="/abonniert">FOLGE ICH</a></li>
                    <li><a id="mein_profil_spender_id" href="/mein_profil_spender">MEIN PROFIL</a></li>
                {% endif %}
                {% if is_granted('ROLE_RECEIVER') %}
                    <li><a id="meine_projekte_id" href="/empfaenger_dashboard">MEINE PROJEKTE</a></li>
                    <li><a id="mein_profil_empfaenger_id" href="/mein_profil_empfaenger">MEIN PROFIL</a></li>
                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right sv-remove-margin">
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li><a href="/logout">LOGOUT</a></li>
                {% else %}
                    <li><a id="login_id" href="/login">ANMELDEN</a></li>
                    <li><a id="register_id" href="/register">REGISTRIEREN</a></li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>